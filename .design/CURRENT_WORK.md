# Current Work

_Last updated: 2026-09-04_

## Status

Site is built, styled and live at <https://jjbits.github.io/>. Migrated from
the 2022 GitHub-Pages Jekyll stub to Hugo + Blowfish, deployed by GitHub
Actions on every push to `main`.

**First post is live as a draft:** `content/posts/dissecting-freetoken/`,
"Dissecting FreeToken". It has one intro paragraph and the seven-row
problems-and-solutions table. Joon is coming back with more content for it.

Drafts now publish. `buildDrafts = true` in `config/_default/hugo.toml`, so a
post with `draft: true` goes live wearing Blowfish's "Draft" badge. Setting
`draft: false` only removes the badge. This is what Joon means by "put it out
with the draft tag".

## Waiting on Joon

- The rest of the FreeToken post. He dictates every reader-facing word; see
  the first principle in CLAUDE.md. Only spelling and grammar may be corrected,
  and each correction is reported back so he can veto it.
- The post's `description`, `summary`, `tags` and `categories` are still empty.
- The archetype's reminder comment is still at the top of the post body; delete
  it once he is done.
- Open decision: the brand orange `#F5A623` on "AI Computations" measures
  2.03:1 against the light theme, below the 3:1 needed for large text. Fine in
  dark mode (7.47:1). `#B87400` (3.79:1) keeps the hue and passes. Left as the
  exact brand colour for now, deliberately.

## What exists

- `config/_default/` — tuned for long technical posts: TOC, reading progress,
  code copy button, tags, 10 posts per page. Title "jjbits", description
  "Journals on computations in AI", both Joon's wording.
- Home page: animated wireframe tetrahedron beside the heading, linking home.
  Heading in Chakra Petch Bold, "AI Computations" in brand orange.
- Post titles in the article lists (home "Recent" and `/posts/`) are sky blue
  `#0284c7`, per Joon; rule in `assets/css/custom.css`.
- Favicons: the tetrahedron mark, generated by `scripts/make-favicons.py` into
  `static/`. Vertex radius `NODE_R = 0.07`, shrunk from 0.085 at Joon's
  request. Regenerate, never hand-edit the PNGs.
- `archetypes/posts.md` — new-post front matter plus the markup gotchas.
- `.github/workflows/deploy.yml` — build + deploy, Hugo pinned to 0.165.0.

## Key context

- The FreeToken table came from the paper-reading session in
  `~/projects/paper-reads/freetoken`. That session cannot message this one
  (its SendMessage is blocked), so it writes to `notes/` there instead;
  the table is `notes/problems-and-solutions.md`, with provenance notes on
  which wording is the paper's and which is its own. Ask it, then read the
  file.
- Two markup failure modes are **silent**: KaTeX needs the `katex` shortcode on
  the page, and Chroma has no `cuda`/`ptx` lexer (use `cpp`). Both in CLAUDE.md.
- Posts sort by the `date` field and `buildFuture = false`, so a future-dated
  post silently will not publish.
- `layouts/partials/home/profile.html` is the one real theme override and must
  be re-diffed on a Blowfish upgrade — see `.design/operations.md`.
- Hugo and Blowfish versions are coupled and must be bumped together.
- `origin` is SSH, not HTTPS: the `gh` OAuth token lacks `workflow` scope and
  cannot push `.github/workflows/`.
- Record how-it-was-built with `ii commit`; the SessionEnd hook auto-distills
  each session into the recipe.
